<!DOCTYPE html>
<html>
<head>
    <title>お子さんを編集</title>
</head>
<body>
    <h1>お子さんを編集</h1>
    <form action="/edit_student" method="post">
        <input type="hidden" name="student_id" value="<%= @student.id %>">
        <label for="name">お子さんの名前:</label>
        <input type="text" name="name" value="<%= @student.name %>" required>
        <br>
        <label for="grade_id">学年:</label>
        <select name="grade_id" id="grade_id" required>
            <option value="1" <%= 'selected' if @student.grade_id == 1 %>>中学一年生</option>
            <option value="2" <%= 'selected' if @student.grade_id == 2 %>>中学二年生</option>
            <option value="3" <%= 'selected' if @student.grade_id == 3 %>>中学三年生</option>
            <option value="4" <%= 'selected' if @student.grade_id == 4 %>>高校一年生</option>
            <option value="5" <%= 'selected' if @student.grade_id == 5 %>>高校二年生</option>
            <option value="6" <%= 'selected' if @student.grade_id == 6 %>>高校三年生</option>
        </select>
        <br>
        <label for="school">学校名:</label>
        <input type="text" name="school" value="<%= @student.school %>" required>
        <br>
        <button type="submit">更新</button>
    </form>
</body>
</html>